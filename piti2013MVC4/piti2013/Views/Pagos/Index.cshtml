@model IEnumerable<piti2013.Models.Pagos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Servicios.serviciosID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fechapago)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.montopago)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Servicios.serviciosID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fechapago)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.montopago)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.pagosID }) |
            @Html.ActionLink("Details", "Details", new { id=item.pagosID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.pagosID })
        </td>
    </tr>
}

</table>
